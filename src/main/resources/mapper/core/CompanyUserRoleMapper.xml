<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.coolplay.system.core.dao.CompanyUserRoleMapper">

    <resultMap id="CompanyUserRole" type="com.coolplay.system.core.model.CompanyUserRoleModel">
        <result property="userId" column="user_id"/>
        <result property="roleId" column="role_id"/>
    </resultMap>

	<!-- 用于select查询公用抽取的列 -->
	<sql id="columns">
	    <![CDATA[
        user_id ,role_id 
	    ]]>
	</sql>

	<sql id="findWhere">
	    <where>
	    </where>
	</sql>

	
	<select id="find" resultMap="CompanyUserRole">
	    SELECT <include refid="columns"/> FROM d_company_user_role 
		<include refid="findWhere"/>
    </select>

	
</mapper>

